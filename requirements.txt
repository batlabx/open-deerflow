# Core orchestration (all open-source, already what DeerFlow uses)
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.3.0

# Local models — the cloud-LLM swap
langchain-ollama>=0.2.0

# MCP: tools as portable, standalone servers
mcp>=1.2.0
langchain-mcp-adapters>=0.1.0

# Tool implementations
httpx>=0.27.0          # search + crawl HTTP
trafilatura>=1.12.0    # boilerplate-free web extraction (Jina reader swap)
ddgs>=6.0.0            # keyless DuckDuckGo search fallback
pydantic>=2.7.0        # structured planner output
